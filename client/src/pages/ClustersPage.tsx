import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

export default function ClustersPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Кластеры</CardTitle>
        <CardDescription>Группы связанных узлов — скоро.</CardDescription>
      </CardHeader>
    </Card>
  )
}
