import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

export default function GraphPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Схема сети</CardTitle>
        <CardDescription>Интерактивная визуализация графа переводов — скоро.</CardDescription>
      </CardHeader>
    </Card>
  )
}
