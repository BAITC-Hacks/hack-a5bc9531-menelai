import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

export default function TopPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Топ приоритетов</CardTitle>
        <CardDescription>Рейтинг узлов для проверки — скоро.</CardDescription>
      </CardHeader>
    </Card>
  )
}
