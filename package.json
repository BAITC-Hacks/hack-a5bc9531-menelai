{
  "name": "graf-deneg",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "cd client && bun run build",
    "start": "cd server && bun run start"
  }
}
